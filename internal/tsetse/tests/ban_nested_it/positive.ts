describe('given', () => {
	it('should be okay', () => {
		expect(1).toBeTruthy();
	});
});

describe('given', () => {
	describe('when', () => {
		describe('and', () => {
			it('should be okay', () => {
				expect(1).toBeTruthy();
			});
		});
	});
});
